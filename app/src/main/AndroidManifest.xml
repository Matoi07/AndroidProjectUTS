<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.adhitya.uts_adhityadwicahyana_10116010">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".kontak_list"></activity>
        <activity android:name=".menu_tambahteman" />
        <activity
            android:name=".Menu_Utama"
            android:label="@string/title_activity_menu__utama" />
        <activity
            android:name=".Menu_List"
            android:label="@string/title_activity_menu__list" />
        <activity
            android:name=".Menu_Kontak"
            android:label="@string/title_activity_menu__kontak" />
        <activity android:name=".viewpage" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>